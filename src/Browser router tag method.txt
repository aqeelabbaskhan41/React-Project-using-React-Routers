//it is best if you don't using loader or useLoaderdata() hook 

<BrowserRouter>
        <Routes>
          <Route path="/" element={<Layout />}>
            <Route path="" element={<Home />} />
            <Route path="about" element={<About />} />
            <Route path="contact" element={<Contact />} />
            <Route 
            // loader={GitHubInfoLoader} it will not work with this syntax so use createbrowser method
            path="github" element={<Github />} />
            <Route path="login" element={<Login />} />
            <Route path="getstarted" element={<Getstarted />} />
          </Route>
        </Routes>
      </BrowserRouter>


// this method is also good but use of loader is prefered when working with Router
  const [data, setData] = useState();
  const url = `https://api.github.com/users/aqeelabbaskhan41`;

  const gitFetch = async () => {
    const response = await fetch(url);
    const result = await response.json();
    console.log(result);
    setData(result);
  };

  useEffect(() => {
    gitFetch(); // Fetch data when the component mounts
  }, []);